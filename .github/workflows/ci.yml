name: CI

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 7 * * 1"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changeset-required:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
        with:
          version: 10.4.1
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Check changeset requirement
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          rm -f .artifacts/failure-summary.md .artifacts/error.log || true
          if ! node scripts/check-changeset-required.mjs > .artifacts/error.log 2>&1; then
            {
              echo "### changeset-required"
              echo ""
              echo '```text'
              tail -n 80 .artifacts/error.log
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
      - name: Persist failure digest artifact
        if: failure()
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          if [ ! -f .artifacts/failure-summary.md ]; then
            {
              echo "### failure-digest-fallback"
              echo ""
              echo "The job failed before a dedicated failure summary was written."
              echo ""
              echo '```text'
              if [ -f .artifacts/error.log ]; then
                tail -n 120 .artifacts/error.log
              else
                echo "No .artifacts/error.log file was found."
              fi
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ci-changeset-required
          path: .artifacts
          if-no-files-found: ignore

  verify:
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10.4.1
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Verify toolchain
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          rm -f .artifacts/failure-summary.md .artifacts/error.log || true
          if ! (
            pnpm typecheck:compat &&
            pnpm typecheck &&
            pnpm build &&
            pnpm test
          ) > .artifacts/error.log 2>&1; then
            {
              echo "### verify (node ${{ matrix.node-version }})"
              echo ""
              echo '```text'
              tail -n 120 .artifacts/error.log
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
      - name: Persist failure digest artifact
        if: failure()
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          if [ ! -f .artifacts/failure-summary.md ]; then
            {
              echo "### failure-digest-fallback"
              echo ""
              echo "The job failed before a dedicated failure summary was written."
              echo ""
              echo '```text'
              if [ -f .artifacts/error.log ]; then
                tail -n 120 .artifacts/error.log
              else
                echo "No .artifacts/error.log file was found."
              fi
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ci-verify-node-${{ matrix.node-version }}
          path: .artifacts
          if-no-files-found: ignore

  release-integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10.4.1
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Release integrity checks
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          rm -f .artifacts/failure-summary.md .artifacts/error.log || true
          if ! (
            pnpm telegram:baseline:check &&
            pnpm telegram:schema:fetch:ci &&
            pnpm telegram:schema:completeness:check &&
            pnpm telegram:schema:drift:check &&
            pnpm api:snapshot:check &&
            pnpm pack:size &&
            pnpm -r build
          ) > .artifacts/error.log 2>&1; then
            {
              echo "### release-integrity"
              echo ""
              echo '```text'
              tail -n 120 .artifacts/error.log
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
      - name: Persist failure digest artifact
        if: failure()
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          if [ ! -f .artifacts/failure-summary.md ]; then
            {
              echo "### failure-digest-fallback"
              echo ""
              echo "The job failed before a dedicated failure summary was written."
              echo ""
              echo '```text'
              if [ -f .artifacts/error.log ]; then
                tail -n 120 .artifacts/error.log
              else
                echo "No .artifacts/error.log file was found."
              fi
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ci-release-integrity
          path: .artifacts
          if-no-files-found: ignore

  redis-integration:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10.4.1
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Redis integration tests
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          rm -f .artifacts/failure-summary.md .artifacts/error.log || true
          if ! pnpm --filter @jilimb0/tgwrapper-adapter-redis test:integration > .artifacts/error.log 2>&1; then
            {
              echo "### redis-integration"
              echo ""
              echo '```text'
              tail -n 120 .artifacts/error.log
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
        env:
          REDIS_URL: redis://127.0.0.1:6379
      - name: Persist failure digest artifact
        if: failure()
        run: |
          set -euo pipefail
          mkdir -p .artifacts
          if [ ! -f .artifacts/failure-summary.md ]; then
            {
              echo "### failure-digest-fallback"
              echo ""
              echo "The job failed before a dedicated failure summary was written."
              echo ""
              echo '```text'
              if [ -f .artifacts/error.log ]; then
                tail -n 120 .artifacts/error.log
              else
                echo "No .artifacts/error.log file was found."
              fi
              echo '```'
            } | tee .artifacts/failure-summary.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ci-redis-integration
          path: .artifacts
          if-no-files-found: ignore

  summary:
    runs-on: ubuntu-latest
    needs:
      - changeset-required
      - verify
      - release-integrity
      - redis-integration
    if: always()
    steps:
      - name: Prepare artifact download directory
        if: always()
        run: rm -rf .artifacts-upload && mkdir -p .artifacts-upload
      - uses: actions/download-artifact@v6
        if: always()
        continue-on-error: true
        with:
          path: .artifacts-upload
          pattern: ci-*
          merge-multiple: true
      - name: CI summary
        run: |
          set -euo pipefail

          CHANGESET_RESULT="${{ needs.changeset-required.result }}"
          VERIFY_RESULT="${{ needs.verify.result }}"
          RELEASE_RESULT="${{ needs.release-integrity.result }}"
          REDIS_RESULT="${{ needs.redis-integration.result }}"

          {
            echo "## CI Summary"
            echo ""
            echo "| Job | Result |"
            echo "|---|---|"
            echo "| changeset-required | ${CHANGESET_RESULT} |"
            echo "| verify | ${VERIFY_RESULT} |"
            echo "| release-integrity | ${RELEASE_RESULT} |"
            echo "| redis-integration | ${REDIS_RESULT} |"
            echo ""
            echo "## Failure digest"
            echo ""
          } >> "$GITHUB_STEP_SUMMARY"

          FILE_LIST=$(find .artifacts-upload -maxdepth 3 -type f | sort || true)

          {
            echo "### Downloaded artifact files"
            echo ""
            echo '```text'
            if [ -n "$FILE_LIST" ]; then
              printf '%s\n' "$FILE_LIST"
            else
              echo "No files were downloaded into .artifacts-upload."
              echo "Possible causes: no artifacts matched the pattern, artifact upload was skipped, or the source jobs never reached upload-artifact."
            fi
            echo '```'
            echo ""
          } >> "$GITHUB_STEP_SUMMARY"

          if find .artifacts-upload -name 'failure-summary.md' -print -quit >/dev/null 2>&1; then
            while IFS= read -r file; do
              cat "$file" >> "$GITHUB_STEP_SUMMARY"
              echo "" >> "$GITHUB_STEP_SUMMARY"
            done < <(find .artifacts-upload -name 'failure-summary.md' | sort)
          else
            {
              echo "### Failure digest unavailable"
              echo ""
              echo "No failure-summary.md files were found in downloaded artifacts."
              echo "This indicates either artifact upload did not include the digest or the failing job never reached digest creation."
              echo ""
            } >> "$GITHUB_STEP_SUMMARY"
          fi

          for result in "$CHANGESET_RESULT" "$VERIFY_RESULT" "$RELEASE_RESULT" "$REDIS_RESULT"; do
            if [ "$result" = "failure" ]; then
              exit 1
            fi
          done
