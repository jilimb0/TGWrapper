{
  "name": "example-aws-lambda",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "dependencies": {
    "tgwrapper": "^0.3.0"
  },
  "devDependencies": {
    "serverless": "^4.6.2",
    "serverless-esbuild": "^1.55.1"
  }
}
